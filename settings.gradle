pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "com.diffplug.spotless" version "${spotlessVersion}"
        id "io.quarkus" version "${quarkusVersion}"
    }
}

rootProject.name = 'lh-agent-connector'
